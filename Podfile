# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Firebase-Swift-Login' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Firebase-Swift-Login
  pod 'FacebookCore'
  pod 'FacebookLogin'
  pod 'Firebase/Core'
  pod 'Firebase/Auth'
  pod 'Firebase/Database'

  target 'Firebase-Swift-LoginTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Firebase-Swift-LoginUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
